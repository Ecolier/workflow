{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Workflow Input Schema",
  "description": "Schema Input for running a workflow",
  "type": "object",
  "required": ["input"],
  "properties": {
    "input": {
      "type": "string",
      "description": "Array of workflow nodes"
    }
  },
  "additionalProperties": false
}